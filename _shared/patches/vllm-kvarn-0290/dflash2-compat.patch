KVarN <-> DFlash2 drafter compat, re-anchored to vLLM 0.29.0.

Applied by install-dflash2.sh (never by install.sh) on a tree that already has
the DFlash2 W4A16 backport. Three of the port's four hunks survive; the fourth
is retired:

  1. qwen3_dflash.py — REQUIRED. The drafter's sliding-window layers have no
     KVarN backend, so they keep the native cache dtype while the target's
     full-attention layers take kvarn_k4v2_g128.
  2. speculator.py, walk kernel — bound the top-k index the selector walk uses
     (a degenerate score row can hand gumbel_noised_argmax a NaN path).
  3. speculator.py, _generate_draft — NaN-guard the score block before the walk.
  4. RETIRED: the port's `_selector_tokens` empty->zeros hunk. 0.29.0 renamed
     that buffer; `_cached_candidate_ids` is torch.zeros in the stock tree, so
     the fix is upstream and the hunk has no target here.

Hunks 2 and 3 are re-cut against the stock 0.29.0 file (the port's originals
were cut against 0.28.0 and their context no longer exists) — regenerate by
diffing the stock file, never by editing here.

--- hunk 1: exported from cpuchip/vllm 58d20f40d (kvarn-v2-runner-0.29.0) ---

KVarN V2-runner compatibility fixes for vLLM 0.28.0.

These changes are applied after the 0.28 KVarN base port and the repo's DFlash2 patches. They keep sliding-window groups out of prefix-cache arithmetic, use the Mamba group's block size for resumed-state indexing, and harden DFlash2 selector buffers against degenerate scores.

Ported to 0.29.0: the four kv_cache_utils.py hunks are retired. Upstream rewrote that function as
resolve_kv_cache_block_sizes (#51718, #53896): the hash unit is now the GCD over prefix-cacheable groups
only and every spec's per-state alignment is checked, which is what the hunks did by hand for the
drafter's sliding-window groups. On this model the groups are the KVarN full-attention layers and the
Mamba layers at the aligned block (2176 tokens for k4v2_g128) and the drafter's sliding-window layers at
block 128 with their page padded to the primary page, so both trees compute the same scheduler block
(2176) and hash unit (128); the hunks changed nothing for this geometry, and the coordinator hunks below
still exempt a sliding-window block that does not divide the scheduler block.

--- exported from cpuchip/vllm 58d20f40d (kvarn-v2-runner-0.29.0); regenerate with scripts/export-patch.sh, do not edit ---

diff --git a/model_executor/models/qwen3_dflash.py b/model_executor/models/qwen3_dflash.py
index d38adfc88..cbcf78c39 100644
--- a/model_executor/models/qwen3_dflash.py
+++ b/model_executor/models/qwen3_dflash.py
@@ -224,6 +224,15 @@
         )
 
         self.sliding_window = sliding_window
+        # KVarN has no sliding-window backend; keep the drafter's SW layers in
+        # the native cache dtype while the target full-attention layers use KVarN.
+        if cache_config is not None and str(
+            getattr(cache_config, "cache_dtype", "auto")
+        ).startswith("kvarn"):
+            from copy import copy as _kv_copy
+
+            cache_config = _kv_copy(cache_config)
+            cache_config.cache_dtype = "auto"
         self.attn = Attention(
             self.num_heads,
             self.head_dim,

--- hunks 2-3: re-anchored to stock v0.29.0 ---

--- a/v1/worker/gpu/spec_decode/dflash2/speculator.py
+++ b/v1/worker/gpu/spec_decode/dflash2/speculator.py
@@ -64,6 +64,9 @@
             IS_DRAFTING=True,
             USE_FP64=USE_FP64,
         )
+        # A degenerate distribution can produce NaN scores. Keep the walk in
+        # bounds; the verify step rejects any bad proposal (KVarN port).
+        index = tl.where(index >= top_k, 0, index)
 
         tl.store(
             realized_scores_ptr + candidate_base + offsets,
@@ -212,6 +215,7 @@
             hidden_states,
             anchor_token_ids,
         )
+        scores = torch.nan_to_num(scores, nan=-1e30, posinf=1e30, neginf=-1e30)
         self._sample_path(candidate_ids, scores, num_reqs)
         if self.draft_logits is not None:
             self._cache_draft_logits(candidate_ids, num_sample)
